# Hash Integrity for messages

You are to create a Python code to demonstrate message integrity. 
You can use whatever hash function you want but my opinion SHA-256 is easiest. 
The idea is that what string text either user input or set string text 
you have in your code, if I was to make a change and run the code it 
should mention "integrity failed message tampered" output. 
If I do not make any changes to the code and run then it should mention 
"Integrity verified message not altered". I'll leave the message output 
up to you but something close. Submit via email the Python code 
